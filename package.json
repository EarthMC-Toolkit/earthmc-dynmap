{
  "name": "earthmc-dynmap-plus",
  "private": true,
  "type": "module",
  "scripts": {
    "userscript": "bun build-userscript.ts",
    "extension": "bun build-extension.ts",
    "build": "bun run userscript && bun run extension"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "@types/chrome": "^0.2.5",
    "@types/leaflet": "^1.9.22",
    "@archiver/archiver": "^0.1.1",
    "esbuild": "^0.28.2",
    "typescript": "latest"
  },
  "engines": {
    "bun": ">=1.3.0"
  }
}
